---
phase: 01-landing-captura-de-leads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker/Dockerfile
  - docker/config.docker.php
  - tests/e2e.sh
  - .gitignore
  - app/bootstrap.php
  - app/.htaccess
  - app/lib/db.php
  - app/lib/security.php
  - app/lib/session.php
  - app/lib/leads.php
  - app/config/config.example.php
  - app/config/site.php
  - app/database/schema.sql
  - app/storage/.gitkeep
  - app/views/lead-form.php
  - public/_bootstrap.php
  - public/.htaccess
  - public/setup.php
  - public/api/lead.php
  - public/index.php
autonomous: true
requirements: [INST-01, INST-02, LEAD-01, LEAD-02, LEAD-03, LEAD-04, LEAD-05, LEAD-06, LAND-02]

must_haves:
  truths:
    - "Rodar setup.php numa cópia nova cria as tabelas leads, users e rate_limits e o usuário do dono com senha em hash; uma segunda visita ao setup.php responde 403"
    - "Visitante abre / , preenche nome, telefone, e-mail, mensagem, marca o consentimento LGPD e envia; o lead aparece na tabela leads com status 'novo', telefone só com dígitos, consent_at, consent_ip e consent_text"
    - "Após o envio (com ou sem JavaScript) o visitante vê confirmação e um link https://wa.me/ com mensagem pré-preenchida contendo o nome dele"
    - "Interesses marcados (ids de produtos do app/config/site.php) são gravados na coluna interests; ids desconhecidos são rejeitados com 422"
    - "Honeypot preenchido não grava lead; token CSRF inválido responde 403; envio em menos de 3 s responde 422; o 6º envio do mesmo IP em 10 min responde 429"
    - "GET /app/config/config.php não é servido (404/403) e app/.htaccess nega acesso caso app/ fique dentro do webroot"
  artifacts:
    - path: "tests/e2e.sh"
      provides: "Verificação ponta a ponta via docker compose + curl + consultas SQL"
      contains: "E2E OK"
    - path: "app/database/schema.sql"
      provides: "Schema canônico (leads, users, rate_limits) usado pelo setup e pela Fase 2"
      contains: "CREATE TABLE IF NOT EXISTS `rate_limits`"
    - path: "app/lib/security.php"
      provides: "e(), csrf_token(), csrf_verify(), form_ts_issue(), form_ts_age(), client_ip(), rate_limit_hit(), csp_nonce(), send_security_headers(), valid_hex_color()"
      exports: ["csrf_verify", "rate_limit_hit"]
    - path: "app/lib/leads.php"
      provides: "lead_validate(), lead_insert(), whatsapp_url()"
    - path: "public/api/lead.php"
      provides: "Endpoint POST do formulário (JSON ou redirect 303)"
    - path: "public/setup.php"
      provides: "Instalação única com lock em app/storage/setup.lock"
    - path: "app/config/site.php"
      provides: "Marca e conteúdo do mentor (fonte única, LAND-02)"
  key_links:
    - from: "app/views/lead-form.php"
      to: "public/api/lead.php"
      via: "form action=\"api/lead.php\" method=\"post\""
      pattern: "action=\"api/lead.php\""
    - from: "public/api/lead.php"
      to: "leads table"
      via: "lead_insert() com prepared statement"
      pattern: "INSERT INTO leads"
    - from: "public/setup.php"
      to: "app/database/schema.sql"
      via: "leitura do arquivo e execução statement a statement"
      pattern: "schema.sql"
    - from: "public/api/lead.php"
      to: "rate_limits table"
      via: "rate_limit_hit(db(), 'lead', client_ip(), max, window)"
      pattern: "rate_limit_hit"
---

<objective>
Entregar a fatia vertical de captura ponta a ponta: o dono instala uma cópia nova pelo `setup.php` e um visitante envia o formulário de contato (página mínima em `/`) cujo lead fica gravado com segurança no MySQL, com consentimento LGPD, interesses, anti-spam e botão de WhatsApp após o envio. Inclui o ambiente Docker local e o teste e2e que prova tudo isso.

Purpose: é o valor central do produto (lead gravado com segurança). A camada compartilhada (bootstrap, db, sessão, segurança, schema com `users`, coluna `status` e `rate_limits`) nasce aqui já no formato que a Fase 2 (painel) vai reutilizar.
Output: stack Docker, `tests/e2e.sh` passando, `app/` (bootstrap, libs, config, schema, view do formulário), `public/` (setup, API de lead, index mínimo, .htaccess).
</objective>

## Phase Goal

**As a** visitante interessado na mentoria, **I want to** enviar meu nome, telefone, e-mail, mensagem e interesses com consentimento LGPD, **so that** o mentor receba meu contato gravado com segurança e eu possa seguir a conversa pelo WhatsApp.

<execution_context>
@C:/Users/T-GAMER/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/T-GAMER/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-captura-de-leads/01-CONTEXT.md

Projeto greenfield (só `.planning/` e `CLAUDE.md`). Ambiente: Windows + Git Bash, sem PHP/MySQL locais; Docker Desktop com imagens `php:8.2-apache` e `mariadb:10.11` já baixadas.

Refinamento da estrutura do CONTEXT.md (autorizado: "planner pode refinar"): o schema canônico fica em `app/database/schema.sql` (e não em `database/` na raiz), para que o `setup.php` o leia de dentro de `app/` — pasta que já é protegida e que o dono sobe junto na Hostinger. Não criar `database/` na raiz.

<interfaces>
<!-- Contratos que este plano CRIA e que o plano 01-02 e a Fase 2 consomem. Implementar exatamente com estes nomes. -->

public/_bootstrap.php
  - define APP_PATH se não definido: default __DIR__ . '/../app' (comentário em PT explicando trocar para __DIR__ . '/app' quando app/ precisar ficar dentro do public_html)
  - require APP_PATH . '/bootstrap.php'
  - entradas: public/index.php e public/setup.php fazem require __DIR__ . '/_bootstrap.php'; public/api/lead.php faz require __DIR__ . '/../_bootstrap.php'

app/bootstrap.php
  - carrega arquivo de config: getenv('APP_CONFIG_FILE') ?: ($_SERVER['APP_CONFIG_FILE'] ?? APP_PATH . '/config/config.php'); se ausente responde 500 texto puro "Configuração ausente: copie app/config/config.example.php para app/config/config.php." e encerra
  - function app_config(?string $key = null, mixed $default = null): mixed   (chave com ponto: 'db.host', 'rate_limit.lead.max')
  - function site_config(): array   (require APP_PATH . '/config/site.php', cache estático)
  - date_default_timezone_set(app_config('timezone', 'America/Sao_Paulo')); ini display_errors=0, log_errors=1
  - require_once lib/db.php, lib/session.php, lib/security.php, lib/leads.php; chama session_start_secure()

app/lib/db.php
  - function db(): PDO   singleton; DSN mysql:host=;port=;dbname=;charset=utf8mb4; ATTR_ERRMODE=EXCEPTION, DEFAULT_FETCH_MODE=ASSOC, EMULATE_PREPARES=false
  - function now_db(): string   date('Y-m-d H:i:s') no fuso do app (todas as datas gravadas vêm do PHP, para KPIs "hoje/7 dias" da Fase 2 baterem com o fuso)

app/lib/session.php
  - function session_start_secure(): void   nome app_config('session_name','LPMSESSID'); cookie httponly, samesite=Lax, secure quando HTTPS; use_strict_mode=1, use_only_cookies=1
  - function flash_set(string $key, array $data): void
  - function flash_take(string $key): ?array   (lê e remove)

app/lib/security.php
  - function e(?string $s): string   htmlspecialchars ENT_QUOTES|ENT_SUBSTITUTE, UTF-8
  - function csrf_token(): string   gera bin2hex(random_bytes(32)) em $_SESSION['csrf_token'] se ausente
  - function csrf_verify(?string $token): bool   hash_equals
  - function form_ts_issue(): string   "{time}.{hmac_sha256(time, app_secret)}"
  - function form_ts_age(?string $value): ?int   segundos desde a emissão; null se assinatura inválida/ausente
  - function client_ip(): string   $_SERVER['REMOTE_ADDR'] (não confiar em X-Forwarded-For)
  - function rate_limit_hit(PDO $pdo, string $action, string $ip, int $max, int $windowSeconds): bool   true = permitido (registra a tentativa); false = bloqueado; apaga registros com mais de 1 dia de forma oportunista
  - function csp_nonce(): string   por requisição, base64 de random_bytes(16)
  - function send_security_headers(): void   CSP: default-src 'self'; script-src 'self'; style-src 'self' 'nonce-{nonce}' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' data:; connect-src 'self'; form-action 'self'; frame-ancestors 'none'; base-uri 'self'; mais X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, X-Frame-Options DENY
  - function valid_hex_color(?string $value, string $fallback): string   aceita só /^#[0-9a-fA-F]{3}([0-9a-fA-F]{3})?$/

app/lib/leads.php
  - function lead_validate(array $input, array $site): array   retorna ['data' => array, 'errors' => array<string,string>]
  - function lead_insert(PDO $pdo, array $data, array $site, string $ip, string $userAgent): int
  - function whatsapp_url(array $site, string $leadName = ''): string   "https://wa.me/{dígitos}?text=" . rawurlencode(mensagem com {nome} substituído pelo primeiro nome)

app/config/site.php → return array com chaves (todas em inglês, conteúdo em PT-BR):
  brand: [mentor_name, program_name, logo, tagline]
  colors: [primary, accent, background, text]         (hex)
  fonts: [display, body]                              (nomes do Google Fonts)
  seo: [title, description, og_image]
  hero: [eyebrow, headline, subheadline, cta_label]
  about: [title, text (array de parágrafos), photo, highlights (array de strings)]
  products: lista de [id (slug), title, description, price (opcional), link (opcional)]
  testimonials: lista de [name, role, text, photo (opcional)]
  faq: lista de [question, answer]
  whatsapp: [number (E.164 só dígitos, ex. 5511999999999), default_message, after_lead_message (com {nome})]
  lgpd: [consent_text, policy_url, footer_notice]
  social: lista de [label, url]

Contrato HTTP de public/api/lead.php (resposta JSON quando o header Accept contém application/json):
  campos POST: name, phone, email, message, interests[] (ids), consent ("1"), website (honeypot), csrf_token, form_ts
  405 método ≠ POST | 429 {"ok":false,"error":"rate_limited","message":...} | 403 {"ok":false,"error":"csrf","message":...}
  200 honeypot preenchido → {"ok":true,...} SEM gravar
  422 {"ok":false,"error":"too_fast","message":...} quando form_ts_age < app_config('min_fill_seconds',3) ou inválido
  422 {"ok":false,"error":"validation","errors":{campo: mensagem PT}}
  200 {"ok":true,"message":"Recebemos seu contato...","whatsapp_url":"https://wa.me/..."}
  500 {"ok":false,"error":"server","message":...} (sem detalhes internos)
  Sem Accept JSON: sucesso → flash_set('lead', ['status'=>'ok','name'=>primeiro nome]) e 303 Location: ../index.php?enviado=1#contato; erro → flash_set('lead', ['status'=>'error','errors'=>..., 'old'=>...]) e 303 Location: ../index.php#contato
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ambiente Docker e teste e2e da captura (falhando)</name>
  <files>docker-compose.yml, docker/Dockerfile, docker/config.docker.php, tests/e2e.sh, .gitignore</files>
  <read_first>
    - .planning/phases/01-landing-captura-de-leads/01-CONTEXT.md (seções "Banco", "Setup", "Formulário e captura")
    - este plano, bloco interfaces (contrato HTTP de api/lead.php e chaves de config)
  </read_first>
  <action>
Criar `docker/Dockerfile` a partir de `php:8.2-apache`: `docker-php-ext-install pdo_mysql`, `a2enmod headers rewrite`, trocar `AllowOverride None` por `AllowOverride All` em `/etc/apache2/apache2.conf` (sed) e adicionar `PassEnv APP_CONFIG_FILE` num conf habilitado em `/etc/apache2/conf-enabled/`. Não usar pacotes externos (só extensões embutidas).

Criar `docker-compose.yml` com serviços `db` (image `mariadb:10.11`, env MARIADB_DATABASE=lpm, MARIADB_USER=lpm, MARIADB_PASSWORD=lpm_dev_pass, MARIADB_ROOT_PASSWORD=root_dev_pass, healthcheck `healthcheck.sh --connect --innodb_initialized`, volume nomeado `dbdata`) e `web` (build `docker/Dockerfile`, portas `${LPM_PORT:-8080}:80`, volumes `./public:/var/www/html`, `./app:/var/www/app`, `./docker:/var/www/docker:ro`, env `APP_CONFIG_FILE=/var/www/docker/config.docker.php`, depends_on db com condition service_healthy). Assim o docroot é `public/` e `app/` fica fora dele, como na Hostinger.

Criar `docker/config.docker.php` no formato de `config.example.php` (ver Task 2): db host `db`, port 3306, name `lpm`, user `lpm`, pass `lpm_dev_pass`; `app_secret` fixo de 64 hex só para dev; `setup_token` = `docker-setup-token-0123456789`; timezone `America/Sao_Paulo`; `min_fill_seconds` 3; `rate_limit.lead` = max 5, window 600.

Criar `.gitignore` com `app/config/config.php`, `app/storage/*` exceto `app/storage/.gitkeep`.

Criar `tests/e2e.sh` (bash, `set -euo pipefail`, `export MSYS_NO_PATHCONV=1` para Git Bash no Windows, sem jq — parse via grep/sed). Helpers: `fail()`, `assert_eq(esperado, obtido, descrição)`, `sql()` que roda `docker compose exec -T db mariadb -ulpm -plpm_dev_pass lpm -N -B -e "$1"`, `get_form(url, cookiejar)` que baixa o HTML e extrai `csrf_token` e `form_ts` dos inputs hidden (`name="csrf_token" value="..."`). Roteiro, na ordem:
1. `rm -f app/storage/setup.lock`; `docker compose down -v`; `docker compose up -d --build --wait`; aguardar `GET /` responder 200 (até 30 tentativas, 1 s).
2. Proteção: `GET /app/config/config.php` ≠ 200; `GET /_bootstrap.php` = 403; `GET /.htaccess` ≠ 200.
3. Setup: GET `/setup.php` (cookie jar), POST email=dono@example.com, password=SenhaForte123!, password_confirm igual, setup_token do docker, csrf_token → HTTP 200 e corpo contém "Instalação concluída"; `SELECT COUNT(*) FROM users` = 1; `SELECT password_hash LIKE '$2y$%' OR password_hash LIKE '$argon2%' FROM users` = 1; `SHOW TABLES` contém leads, users, rate_limits; POST com setup_token errado (antes do lock) é coberto implicitamente — após a instalação, novo GET `/setup.php` = 403.
4. Lead feliz (JSON): get_form `/`, `sleep 3`, POST `/api/lead.php` com header `Accept: application/json`, name="Ana Souza", phone="(11) 98765-4321", email=ana@example.com, message="Quero saber da mentoria", interests[]=primeiro id de produto do site.php (extrair do HTML: primeiro `name="interests[]" value="..."`), consent=1, website vazio → HTTP 200, corpo contém `"ok":true` e `wa.me`; SQL: phone = 11987654321, status = novo, consent_at não nulo, consent_ip não vazio, consent_text não vazio, interests contém o id.
5. Lead sem JS: novo get_form, sleep 3, POST sem Accept JSON → HTTP 303 e `Location` contém `enviado=1`; seguir com o mesmo cookie `GET /index.php?enviado=1` contém `wa.me` e "Ana" ou o nome enviado. Total de leads = 2.
6. Antes de cada cenário negativo executar `sql "DELETE FROM rate_limits"`. Honeypot: website=http://spam.example → HTTP 200 e total de leads continua 2. CSRF: csrf_token=invalido → 403, total 2. Rápido demais: get_form e POST imediato → 422 com `too_fast`. Validação: consent ausente, email=nao-e-email, phone=123, interests[]=produto-inexistente → 422 e corpo contém `"consent"`, `"email"`, `"phone"`, `"interests"`; total continua 2.
7. Rate limit: `DELETE FROM rate_limits`; 5 POSTs inválidos seguidos (com CSRF e sleep já feitos) → 422 cada; 6º → 429.
8. Imprimir `E2E OK`. Deixar o stack de pé (para verificação humana); flag opcional `--down` derruba no final.

Neste ponto o script DEVE falhar (ainda não existe app nem public) — é o teste vermelho da fatia.
  </action>
  <verify>
    <automated>bash tests/e2e.sh; test $? -ne 0 && echo "RED OK (falha esperada antes das Tasks 2-3)"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` roda sem erro e lista os serviços `web` e `db`
    - `grep -c "E2E OK" tests/e2e.sh` ≥ 1; `grep -c "rate_limits" tests/e2e.sh` ≥ 1; `grep -c "429" tests/e2e.sh` ≥ 1; `grep -c "MSYS_NO_PATHCONV" tests/e2e.sh` ≥ 1
    - `grep -c "AllowOverride All" docker/Dockerfile` ≥ 1 e `grep -c "app/config/config.php" .gitignore` = 1
    - Rodar `bash tests/e2e.sh` agora termina com código ≠ 0
  </acceptance_criteria>
  <done>Stack Docker sobe e o e2e existe cobrindo setup, lead feliz (JSON e sem JS), honeypot, CSRF, tempo mínimo, validação e rate limit — falhando por falta de implementação.</done>
</task>

<task type="auto">
  <name>Task 2: Instalação da cópia — camada compartilhada, schema e setup.php</name>
  <files>public/_bootstrap.php, public/.htaccess, app/.htaccess, app/bootstrap.php, app/lib/db.php, app/lib/session.php, app/lib/security.php, app/lib/leads.php, app/config/config.example.php, app/database/schema.sql, app/storage/.gitkeep, public/setup.php</files>
  <read_first>
    - .planning/phases/01-landing-captura-de-leads/01-CONTEXT.md (seções "Banco", "Setup (INST-01/02)", "Stack e hospedagem")
    - este plano, bloco interfaces (assinaturas exatas)
    - tests/e2e.sh (passos 2 e 3 — o que o setup precisa responder)
    - docker/config.docker.php (formato das chaves)
  </read_first>
  <action>
Implementar os arquivos da camada compartilhada exatamente com as assinaturas do bloco interfaces (INST-02, base para a Fase 2). `app/bootstrap.php` faz `require_once` direto de `app/lib/leads.php`; nesta task criar `app/lib/leads.php` apenas com as três funções do contrato lançando `RuntimeException('não implementado')` — a Task 3 substitui pelo código real.

`app/config/config.example.php`: `return [...]` com chaves `db` (host, port, name, user, pass), `app_secret` (comentário: gerar com 64 caracteres aleatórios), `setup_token` (obrigatório, mín. 16 caracteres, comentário explicando que protege o setup.php contra terceiros antes do dono instalar), `timezone` 'America/Sao_Paulo', `session_name` 'LPMSESSID', `min_fill_seconds` 3, `rate_limit` => ['lead' => ['max' => 5, 'window' => 600], 'login' => ['max' => 5, 'window' => 900]] (a chave login é consumida pela Fase 2). Comentários em PT-BR orientando copiar para `config.php`.

`app/database/schema.sql` (utf8mb4 / utf8mb4_unicode_ci, InnoDB, statements separados por `;` em fim de linha, sem procedures):
- `leads`: id INT UNSIGNED AUTO_INCREMENT PK, name VARCHAR(120) NOT NULL, phone VARCHAR(20) NOT NULL, email VARCHAR(190) NOT NULL, message TEXT NOT NULL, interests TEXT NULL (JSON), status ENUM('novo','contatado','fechado') NOT NULL DEFAULT 'novo', consent_at DATETIME NOT NULL, consent_ip VARCHAR(45) NOT NULL, consent_text TEXT NOT NULL, ip VARCHAR(45) NOT NULL, user_agent VARCHAR(255) NULL, created_at DATETIME NOT NULL, índices idx_leads_created_at, idx_leads_email, idx_leads_status.
- `users`: id INT UNSIGNED AUTO_INCREMENT PK, email VARCHAR(190) NOT NULL UNIQUE, password_hash VARCHAR(255) NOT NULL, created_at DATETIME NOT NULL, last_login_at DATETIME NULL.
- `rate_limits`: id BIGINT UNSIGNED AUTO_INCREMENT PK, ip VARCHAR(45) NOT NULL, action VARCHAR(32) NOT NULL, created_at DATETIME NOT NULL, índice idx_rl_ip_action_created (ip, action, created_at).
Todos com `CREATE TABLE IF NOT EXISTS`.

`public/setup.php` (INST-01): chama `send_security_headers()`. Se `APP_PATH . '/storage/setup.lock'` existe → HTTP 403 com página "Instalação já concluída." e encerra. Se `app_config('setup_token')` vazio ou < 16 chars → página de erro pedindo para definir em config.php. GET mostra formulário PT-BR (e-mail do dono, senha, confirmação, token de instalação, csrf_token hidden). POST: `csrf_verify`, `hash_equals` do setup_token, e-mail via `filter_var FILTER_VALIDATE_EMAIL`, senha ≥ 10 chars e igual à confirmação; conectar com `db()` em try/catch (erro genérico "Não foi possível conectar ao banco. Verifique app/config/config.php." sem expor exceção); ler `APP_PATH . '/database/schema.sql'`, dividir por `;` seguido de quebra de linha, executar cada statement não vazio; se `users` já tiver registro não criar outro; senão `INSERT INTO users (email, password_hash, created_at)` com `password_hash($senha, PASSWORD_DEFAULT)` e `now_db()` via prepared statement; gravar o lock com `file_put_contents(..., LOCK_EX)` contendo a data (erro claro se `app/storage` não for gravável); responder 200 com "Instalação concluída" e recomendação de apagar o setup.php do servidor. Todo output com `e()`.

`public/.htaccess`: `Options -Indexes`; negar dotfiles e arquivos iniciados por `_` (`<FilesMatch "^(\.|_)">` Require all denied); negar extensões sql/lock/md/example; `mod_headers` com X-Content-Type-Options nosniff e Referrer-Policy; cache longo (`Cache-Control max-age=2592000`) para css/js/svg/png/jpg/webp/woff2 via `mod_expires` ou headers, tudo dentro de `<IfModule>`.
`app/.htaccess`: `Require all denied` (proteção para o caso de `app/` ficar dentro do public_html). `app/storage/.gitkeep` vazio.

Nenhum comentário do código deve mencionar fase/plano do GSD.
  </action>
  <verify>
    <automated>docker compose up -d --build --wait && bash -c 'export MSYS_NO_PATHCONV=1; rm -f app/storage/setup.lock; code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:${LPM_PORT:-8080}/setup.php); test "$code" = 200 && echo SETUP_GET_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS" app/database/schema.sql` = 3 e o arquivo contém `ENUM('novo','contatado','fechado')` e `utf8mb4_unicode_ci`
    - `grep -c "password_hash(" public/setup.php` ≥ 1; `grep -c "setup.lock" public/setup.php` ≥ 1; `grep -c "hash_equals" public/setup.php` ≥ 1
    - `grep -c "Require all denied" app/.htaccess` ≥ 1 e `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/_bootstrap.php` = 403
    - `grep -rniE "phase|plan 0|gsd" app public` não retorna nada
  </acceptance_criteria>
  <done>Setup cria as 3 tabelas e o dono com hash, grava o lock e passa a responder 403; credenciais ficam em app/config (fora do docroot e negado por .htaccess); passos 1–3 do e2e passam.</done>
</task>

<task type="auto">
  <name>Task 3: Captura do lead — config da marca, formulário, API e confirmação com WhatsApp</name>
  <files>app/config/site.php, app/lib/leads.php, app/views/lead-form.php, public/api/lead.php, public/index.php</files>
  <read_first>
    - .planning/phases/01-landing-captura-de-leads/01-CONTEXT.md (seções "Configuração da marca", "Formulário e captura")
    - este plano, bloco interfaces (chaves de site.php e contrato HTTP)
    - app/lib/security.php, app/lib/session.php, app/bootstrap.php (criados na Task 2)
    - tests/e2e.sh (passos 4–7)
  </read_first>
  <action>
`app/config/site.php` (LAND-02): `return [...]` com TODAS as chaves do bloco interfaces, preenchido com um mentor fictício realista em PT-BR (sem lorem ipsum), p.ex. mentora "Marina Costa", programa "Rota Clara" para profissionais autônomas estruturarem e precificarem serviços. Cores sóbrias sem roxo: primary `#1F4D3A`, accent `#C8553D`, background `#F6F3EE`, text `#1B1F23`. Fontes: display `Archivo`, body `Inter Tight`. 3 produtos com ids slug (`mentoria-individual`, `grupo-aceleracao`, `diagnostico-express`), 3 depoimentos, 5 perguntas de FAQ, WhatsApp `5511999999999` com `after_lead_message` "Olá, Marina! Sou {nome}, acabei de enviar meu contato pelo site." e textos LGPD (`consent_text` "Autorizo o uso dos meus dados para contato sobre a mentoria, conforme a Política de Privacidade.", `footer_notice` "Seus dados são usados apenas para contato sobre a mentoria."). Logo/foto apontam para `assets/img/logo.svg` e `assets/img/mentor.svg` (criados no plano seguinte). Comentários PT-BR explicando cada bloco e que seções vazias não aparecem.

`app/lib/leads.php` (substitui os stubs): `lead_validate` — trim em tudo; name 2–120 chars (`mb_strlen`); phone = só dígitos (`preg_replace('/\D+/', '')`), aceita 10 ou 11 dígitos; email `filter_var FILTER_VALIDATE_EMAIL` e ≤ 190; message 1–2000 chars; consent deve ser "1"; interests opcional: array de strings, cada uma precisa existir em `array_column($site['products'], 'id')` senão erro em `interests`, deduplicar; gravar em `data['interests']` como JSON (`json_encode` com `JSON_UNESCAPED_UNICODE`) de lista de objetos `{id, title}` ou null se vazio. Mensagens de erro em PT-BR por campo. `lead_insert` — `INSERT INTO leads (name, phone, email, message, interests, status, consent_at, consent_ip, consent_text, ip, user_agent, created_at)` com prepared statement, status 'novo', consent_at e created_at = `now_db()`, consent_ip = ip, consent_text = `$site['lgpd']['consent_text']` (texto do servidor, nunca vindo do cliente), user_agent truncado em 255 (`mb_substr`); retorna `lastInsertId`. `whatsapp_url` conforme interfaces (primeiro nome = primeira palavra do nome; sem nome usa `default_message`).

`public/api/lead.php` (LEAD-01..06): `send_security_headers()`; decidir modo JSON se `$_SERVER['HTTP_ACCEPT']` contém `application/json`; ordem de checagem: método POST (405) → `rate_limit_hit(db(), 'lead', client_ip(), app_config('rate_limit.lead.max',5), app_config('rate_limit.lead.window',600))` (429) → `csrf_verify($_POST['csrf_token'])` (403) → honeypot `website` não vazio (fingir sucesso, 200, não gravar) → `form_ts_age` null ou < `min_fill_seconds` (422 too_fast) → `lead_validate` (422 validation) → `lead_insert` em try/catch (500 genérico, `error_log` da exceção) → sucesso 200 com `message` e `whatsapp_url`. Respostas JSON com `Content-Type: application/json; charset=utf-8` e `JSON_UNESCAPED_UNICODE`. Modo sem JS: usar `flash_set('lead', ...)` e `303` para `../index.php?enviado=1#contato` (sucesso) ou `../index.php#contato` (erro, com `errors` e `old` sem o csrf). Após sucesso, `session_regenerate_id(true)` não é necessário; manter o mesmo token CSRF.

`app/views/lead-form.php` (partial incluído por index.php; recebe `$site`, `$flash`): se `$flash['status'] === 'ok'` renderiza bloco `<div class="lead-success" data-lead-success>` com título "Recebemos seu contato", texto e link `<a class="btn btn-whatsapp" href="{whatsapp_url(site, nome)}" target="_blank" rel="noopener">Falar no WhatsApp</a>`; senão renderiza `<form id="lead-form" data-lead-form action="api/lead.php" method="post">` com inputs `name` (autocomplete name, required, maxlength 120), `phone` (type tel, inputmode tel, required), `email` (type email, required, maxlength 190), `message` (textarea required maxlength 2000), fieldset "Tenho interesse em" com checkbox `interests[]` por produto (value = id) só se houver produtos, checkbox `consent` value 1 required com `consent_text` + link `policy_url`, honeypot `website` dentro de um wrapper `aria-hidden="true"` com `tabindex="-1"` e `autocomplete="off"` escondido por CSS (classe `hp-field`, fora da tela, nunca `type=hidden`), hidden `csrf_token` = `csrf_token()` e `form_ts` = `form_ts_issue()`, `<p class="field-error" id="err-{campo}" data-error-for="{campo}">` para cada campo (preenchido com erros do flash e `aria-invalid`/`aria-describedby` no input), `<p class="form-status" data-form-status role="status" aria-live="polite">`, botão submit "Enviar contato". Valores `old` repovoados com `e()`. Todo conteúdo dinâmico com `e()`.

`public/index.php` (mínimo nesta fatia; o plano 01-02 o expande para a landing completa): require `_bootstrap.php`, `send_security_headers()`, `$site = site_config()`, `$flash = flash_take('lead')`, HTML5 `lang="pt-BR"` com `<title>` do `seo.title`, `<section id="contato">` com título e `require APP_PATH . '/views/lead-form.php'`, e rodapé com `lgpd.footer_notice`.

Ao final, rodar `bash tests/e2e.sh` até imprimir `E2E OK`.
  </action>
  <verify>
    <automated>bash tests/e2e.sh 2>&1 | tail -n 5 | grep -q "E2E OK" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/e2e.sh` termina com código 0 e imprime `E2E OK`
    - `grep -c "INSERT INTO leads" app/lib/leads.php` = 1 e nenhum `$_POST`/`$_GET` interpolado em string SQL (`grep -nE "query\(.*\\\$_(POST|GET)" -r app public` vazio)
    - `consent_text` gravado vem de `$site['lgpd']` (não de `$input`) e `grep -c "rawurlencode" app/lib/leads.php` ≥ 1
    - `php -l` de todos os arquivos PHP sem erros: `docker compose exec -T web sh -c 'for f in $(find /var/www/html /var/www/app -name "*.php"); do php -l $f || exit 1; done'`
  </acceptance_criteria>
  <done>Visitante envia o formulário em `/` (com ou sem JS) e o lead é gravado com consentimento, IP, interesses e status 'novo'; vê confirmação com botão de WhatsApp pré-preenchido; spam é barrado por honeypot, CSRF, tempo mínimo e rate limit; e2e inteiro verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → public/api/lead.php | Entrada não confiável (campos do formulário, headers, possível bot) |
| navegador → public/setup.php | Endpoint administrativo exposto numa cópia recém-publicada |
| webroot → app/ | Arquivos de config/credenciais não podem ser servidos pelo Apache |
| PHP → MySQL | Dados do lead entram em SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SQL (lead_insert, setup) | mitigate | Só PDO prepared statements, EMULATE_PREPARES=false |
| T-01-02 | Spoofing/DoS | api/lead.php | mitigate | CSRF (hash_equals), honeypot, form_ts HMAC ≥3 s, rate_limit_hit 5/600 s por REMOTE_ADDR |
| T-01-03 | Elevation of Privilege | setup.php | mitigate | setup_token obrigatório + setup.lock → 403; password_hash PASSWORD_DEFAULT |
| T-01-04 | Information Disclosure | app/config, erros | mitigate | app/ fora do docroot + `Require all denied`; display_errors=0, mensagens genéricas |
| T-01-05 | Tampering (XSS) | index.php, lead-form.php | mitigate | e() em todo output + CSP com nonce |
| T-01-06 | Repudiation (LGPD) | leads.consent_* | mitigate | consent_at, consent_ip e consent_text do servidor gravados |
| T-01-SC | Tampering | dependências | accept | Nenhum pacote instalado; só imagens oficiais já baixadas |
</threat_model>

<verification>
- `bash tests/e2e.sh` imprime `E2E OK` (setup, lead JSON, lead sem JS, honeypot, CSRF, tempo mínimo, validação, rate limit, proteção de app/)
- `docker compose exec -T db mariadb -ulpm -plpm_dev_pass lpm -e "SELECT id,name,phone,status,interests,consent_at,consent_ip FROM leads"` mostra os leads do teste
</verification>

<success_criteria>
- Cópia nova é instalada rodando setup.php (tabelas + dono) e o setup se bloqueia depois
- Lead válido é gravado com todos os campos, consentimento e interesses; visitante recebe link de WhatsApp pré-preenchido
- Abusos (honeypot, CSRF, envio rápido, excesso por IP) são barrados sem gravar
- Camada compartilhada (bootstrap, db, sessão, segurança, schema com users/status/rate_limits) pronta para o painel
</success_criteria>

<output>
Create `.planning/phases/01-landing-captura-de-leads/01-01-SUMMARY.md` when done
</output>
