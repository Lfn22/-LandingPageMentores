---
phase: 02-painel-do-dono
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e-admin.sh
  - app/lib/auth.php
  - app/lib/admin.php
  - public/admin/index.php
  - public/admin/logout.php
  - app/views/admin/login.php
  - app/views/admin/panel.php
  - public/assets/css/admin.css
autonomous: true
requirements: [PAIN-01, PAIN-02, PAIN-04, PAIN-05, PAIN-06, PAIN-09]

must_haves:
  truths:
    - "Dono entra em /admin/ com e-mail e senha; senha errada mostra 'E-mail ou senha inválidos' e a 6ª tentativa na janela recebe 429"
    - "Logado, o dono vê os KPIs (total, 7 dias, hoje), o Radar de demanda e a Fila de trabalho com os leads mais recentes primeiro"
    - "Busca ?q= filtra por nome, e-mail ou demanda"
    - "Após 'Sair' (POST com CSRF), /admin/ volta a mostrar só o login"
    - "Painel usa fundo azul-ardósia escuro, card branco arredondado e rodapé LGPD, como na referência"
  artifacts:
    - path: "app/lib/auth.php"
      provides: "auth_login, auth_user, auth_require, auth_logout, admin_headers"
    - path: "app/lib/admin.php"
      provides: "admin_kpis, admin_radar, admin_leads, format_phone"
    - path: "public/admin/index.php"
      provides: "GET login/painel + POST login"
    - path: "tests/e2e-admin.sh"
      provides: "e2e do painel (roda tests/e2e.sh antes)"
  key_links:
    - from: "public/admin/index.php"
      to: "rate_limit_hit(db(), 'login', ...)"
      via: "chamada antes de auth_login"
      pattern: "rate_limit_hit\\(.*'login'"
    - from: "app/lib/auth.php"
      to: "users.password_hash"
      via: "password_verify + session_regenerate_id(true)"
      pattern: "session_regenerate_id\\(true\\)"
---

<objective>
Fatia vertical: o dono faz login seguro em `/admin/`, vê KPIs, Radar de demanda e a Fila de trabalho (com busca) no visual da referência, e faz logout.

Purpose: entregar o Core Value do lado do dono — ver os leads com segurança.
Output: `app/lib/auth.php`, `app/lib/admin.php`, `public/admin/{index,logout}.php`, views em `app/views/admin/`, `admin.css`, `tests/e2e-admin.sh`.
</objective>

<execution_context>
@C:/Users/T-GAMER/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/T-GAMER/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-painel-do-dono/02-CONTEXT.md
@.planning/phases/01-landing-captura-de-leads/01-01-SUMMARY.md
@.planning/references/README.md

<interfaces>
Já existem (reusar, não alterar):
- app/bootstrap.php: app_config(key, default), site_config(): array; já chama session_start_secure(). NÃO carrega auth.php/admin.php — as páginas do admin fazem require_once APP_PATH . '/lib/auth.php' e '/lib/admin.php' (não editar bootstrap.php).
- public/_bootstrap.php: define APP_PATH e requer bootstrap. Em public/admin/: require __DIR__ . '/../_bootstrap.php'.
- db(): PDO (FETCH_ASSOC, exceptions); now_db(): 'Y-m-d H:i:s' no fuso America/Sao_Paulo.
- e(), csrf_token(), csrf_verify(?string), client_ip(), rate_limit_hit(PDO, action, ip, max, window): bool (false = bloqueado), send_security_headers() (CSP: script-src 'self'; style-src 'self' + nonce + fonts.googleapis.com; font-src fonts.gstatic.com — sem inline script/style).
- config: rate_limit.login = {max: 5, window: 900}.
- Tabelas: users(id, email, password_hash, created_at, last_login_at); leads(id, name, phone[dígitos], email, message, interests[JSON [{id,title}] ou NULL], status ENUM novo/contatado/fechado, consent_at, consent_ip, created_at).
- site_config(): products[] {id, title}; lgpd.footer_notice.
- tests/e2e.sh: sobe o Docker, roda setup (dono@example.com / SenhaForte123!), deixa leads "Ana Souza" (com interesse) e "Bruno Lima". Plano 01-02 pode editar tests/e2e.sh em paralelo — NÃO editá-lo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e do painel (RED)</name>
  <files>tests/e2e-admin.sh</files>
  <read_first>tests/e2e.sh</read_first>
  <action>Criar `tests/e2e-admin.sh` (bash, set -euo pipefail, MSYS_NO_PATHCONV=1, BASE com LPM_PORT default 8091). Primeiro roda `bash tests/e2e.sh` (sobe stack + setup + leads). Usa diretório relativo próprio `tests/.e2e-admin-tmp/` (mesma razão do e2e.sh: caminhos absolutos quebram no curl do Windows) e copia os helpers fail/assert_eq/assert_contains/sql/http_code e uma função `csrf_from <html>` que extrai `name="csrf_token" ... value`. Sem acentos nos valores de --data-urlencode. Passos: (a) GET /admin/ sem sessão → 200, contém `name="password"`, header `X-Robots-Tag: noindex`, não contém "Fila de trabalho"; (b) `DELETE FROM rate_limits`, POST /admin/ com senha errada → corpo contém "E-mail ou senha inv" e não "Fila de trabalho"; (c) POST com dono@example.com/SenhaForte123! → 303; GET /admin/ contém "Inscritos no total", "Radar de demanda", "Fila de trabalho", "Ana Souza", "Bruno Lima"; `last_login_at` não nulo; (d) GET /admin/?q=bruno contém "Bruno Lima" e não "Ana Souza"; (e) POST /admin/logout.php com csrf → 303; GET /admin/ com o mesmo jar não contém "Fila de trabalho"; (f) `DELETE FROM rate_limits`, 5 POSTs de senha errada, 6º → 429. Terminar com "E2E ADMIN OK". Rodar e confirmar que falha (sem código ainda).</action>
  <verify><automated>LPM_PORT=8091 bash tests/e2e-admin.sh; test $? -ne 0</automated></verify>
  <done>
    - Script existe, roda tests/e2e.sh primeiro e cobre os passos (a)–(f)
    - Falha antes da implementação (RED)
  </done>
</task>

<task type="auto">
  <name>Task 2: auth + login/logout</name>
  <files>app/lib/auth.php, public/admin/index.php, public/admin/logout.php, app/views/admin/login.php</files>
  <read_first>app/lib/security.php, app/lib/session.php, public/api/lead.php</read_first>
  <action>`app/lib/auth.php` (constante AUTH_IDLE_SECONDS = 7200): `admin_headers()` chama send_security_headers() + `X-Robots-Tag: noindex, nofollow` + `Cache-Control: no-store`; `auth_login(PDO, email, password): bool` busca user por e-mail (prepared), faz password_verify (se usuário não existe, verifica contra um hash fixo para tempo constante), no sucesso `session_regenerate_id(true)`, grava `$_SESSION['uid']` e `['last_seen']=time()`, remove `csrf_token` da sessão (novo token pós-login) e atualiza `last_login_at = now_db()`; `auth_user(): ?int` retorna uid se `last_seen` dentro de AUTH_IDLE_SECONDS (renova last_seen), senão chama auth_logout() e retorna null; `auth_require(bool $json = false)` sem usuário → JSON 401 `{"ok":false,"error":"auth"}` ou 302 Location `/admin/` e exit; `auth_logout()` limpa $_SESSION, expira o cookie da sessão (session_get_cookie_params) e session_destroy(). `public/admin/index.php`: require _bootstrap + auth.php + admin.php; admin_headers(); POST (login): ordem rate_limit_hit(db(),'login',client_ip(), app_config('rate_limit.login.max',5), app_config('rate_limit.login.window',900)) → false: 429 + login com "Muitas tentativas. Tente novamente mais tarde."; csrf_verify falha → 403; auth_login falha → 401 + "E-mail ou senha inválidos" (mesma mensagem para usuário inexistente); sucesso → 303 `/admin/`. GET: sem auth_user() → render `app/views/admin/login.php` (form POST com csrf_token, email, password, meta robots noindex, link admin.css); com usuário → render panel (Task 3). `public/admin/logout.php`: só POST (senão 405), auth_require(), csrf_verify ou 403, auth_logout(), 303 `/admin/`.</action>
  <verify><automated>docker compose exec -T web sh -c "php -l /var/www/app/lib/auth.php && php -l /var/www/html/admin/index.php && php -l /var/www/html/admin/logout.php"</automated></verify>
  <done>
    - Login correto regenera sessão e redireciona 303; errado mostra mensagem genérica
    - 6ª tentativa na janela → 429 (rate_limit 'login')
    - Logout exige POST + CSRF e invalida a sessão
    - Toda resposta do admin envia X-Robots-Tag noindex e Cache-Control no-store
  </done>
</task>

<task type="auto">
  <name>Task 3: painel (KPIs, radar, fila com busca) no visual da referência</name>
  <files>app/lib/admin.php, app/views/admin/panel.php, public/assets/css/admin.css, public/admin/index.php</files>
  <read_first>.planning/references/painel-admin-ref.png, app/config/site.php, app/lib/leads.php</read_first>
  <action>`app/lib/admin.php`: `admin_kpis(PDO): array` {total, week (created_at >= agora-7 dias), today (created_at >= date('Y-m-d 00:00:00'))}, datas via PHP no fuso da app (coerente com now_db); `admin_radar(PDO, array $site): array` lê `interests` não nulos, json_decode, conta por id e usa o title de site.php products (fallback: title gravado), ordenado desc; `admin_leads(PDO, string $q, int $limit = 200): array` SELECT id,name,phone,email,message,interests,status,created_at ORDER BY created_at DESC, id DESC, com `q` (trim, máx 100 chars) em `name LIKE ? OR email LIKE ? OR phone LIKE ? OR interests LIKE ?` parametrizado (escapar % e _ no termo); `format_phone(string): string` → "(11) 98765-4321". `app/views/admin/panel.php` (tudo via e()): pill "Painel administrativo"; card com título e, à direita, botão "Copiar CSV" (`data-copy-csv`, sem ação ainda), link "Baixar CSV" para `/admin/export.php` e form POST "Sair" para logout.php com csrf; 3 KPIs (número grande, rótulo); "Radar de demanda" (lista título + contagem, ou "Sem demandas registradas ainda."); "Fila de trabalho" com form GET campo `q` placeholder "Buscar por nome, email ou demanda"; tabela Nome, Contato (telefone formatado com tel:, e-mail com mailto:, link WhatsApp `https://wa.me/55{phone}` target _blank rel noopener), Demandas (títulos), Status (texto do status; o select vem no 02-02); abaixo do nome, data d/m/Y H:i e mensagem (`<details>` com resumo); vazio → "Nenhum lead encontrado."; nota "Mostrando os 200 mais recentes" quando atingir o limite. Rodapé com site lgpd.footer_notice. `admin.css`: fundo #263039, card branco radius 16px, bordas 1px #e3e5e8, botões com borda, KPIs em grid 3 colunas (1 no mobile), tabela com rolagem horizontal no mobile, fonte Inter Tight/Archivo via Google Fonts com fallback system-ui, pesos 600 nos títulos, sem gradientes/sombras pesadas/emojis. Ligar o render do painel em index.php. Rodar o e2e até ficar verde.</action>
  <verify><automated>LPM_PORT=8091 bash tests/e2e-admin.sh</automated></verify>
  <done>
    - tests/e2e-admin.sh imprime "E2E ADMIN OK"
    - KPIs, radar (com estado vazio) e fila mais-recentes-primeiro aparecem com dados reais
    - Busca por nome, e-mail ou demanda funciona com LIKE parametrizado
    - Visual segue a referência (fundo escuro, card branco arredondado, rodapé LGPD)
  </done>
</task>

</tasks>

<threat_model>
| ID | STRIDE | Componente | Disposição | Mitigação |
|----|--------|-----------|-----------|-----------|
| T-02-01 | S | login | mitigate | password_verify, mensagem genérica, rate_limit 'login' 5/900s, hash fixo p/ usuário inexistente |
| T-02-02 | S | sessão | mitigate | session_regenerate_id(true) no login, cookie HttpOnly/SameSite=Lax/Secure (já existente), timeout 2h |
| T-02-03 | T | login/logout | mitigate | csrf_verify em todo POST; logout só POST |
| T-02-04 | I | páginas admin | mitigate | auth_require em tudo; noindex + no-store |
| T-02-05 | T | busca q | mitigate | LIKE com prepared statement; saída via e() (XSS) |
</threat_model>

<verification>
`LPM_PORT=8091 bash tests/e2e-admin.sh` imprime "E2E ADMIN OK" (inclui o e2e da Fase 1).
</verification>

<success_criteria>
Dono loga, vê KPIs/radar/fila com busca, sai; sem sessão nada do painel é exibido; força bruta bloqueada.
</success_criteria>

<output>
Criar `.planning/phases/02-painel-do-dono/02-01-SUMMARY.md` ao concluir.
</output>
