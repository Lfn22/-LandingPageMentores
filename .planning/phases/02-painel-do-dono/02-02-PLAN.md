---
phase: 02-painel-do-dono
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/e2e-admin.sh
  - public/admin/export.php
  - public/admin/status.php
  - public/assets/js/admin.js
  - app/lib/admin.php
  - app/views/admin/panel.php
  - public/assets/css/admin.css
autonomous: false
requirements: [PAIN-03, PAIN-07, PAIN-08, PAIN-09]

must_haves:
  truths:
    - "Dono baixa um CSV UTF-8 com BOM e separador ';' com todos os leads, que abre certo no Excel"
    - "Células que começam com = + - @ saem prefixadas com ' (sem formula injection)"
    - "Dono troca o status (novo/contatado/fechado) na Fila de trabalho e ele persiste no banco"
    - "'Copiar CSV' copia o mesmo CSV para a área de transferência e mostra 'Copiado!'"
    - "export.php e status.php sem sessão não entregam dados nem alteram nada"
  artifacts:
    - path: "public/admin/export.php"
      provides: "download CSV"
    - path: "public/admin/status.php"
      provides: "POST troca de status (JSON ou 303)"
    - path: "public/assets/js/admin.js"
      provides: "Copiar CSV + troca de status via fetch"
  key_links:
    - from: "public/assets/js/admin.js"
      to: "/admin/export.php"
      via: "fetch credentials same-origin + navigator.clipboard.writeText"
      pattern: "clipboard\\.writeText"
    - from: "public/admin/status.php"
      to: "leads.status"
      via: "UPDATE parametrizado com whitelist"
      pattern: "UPDATE leads SET status"
---

<objective>
Fatia vertical: o dono exporta/copia os leads em CSV e muda o status de cada lead na Fila de trabalho.

Purpose: fechar o Core Value (leads exportáveis) e a operação diária da fila.
Output: `public/admin/{export,status}.php`, `public/assets/js/admin.js`, select de status no painel, e2e estendido, verificação visual.
</objective>

<execution_context>
@C:/Users/T-GAMER/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/workflows/execute-plan.md
@C:/Users/T-GAMER/.claude/plugins/cache/gsd-plugin/gsd/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-painel-do-dono/02-CONTEXT.md
@.planning/phases/02-painel-do-dono/02-01-SUMMARY.md

<interfaces>
Do 02-01 (usar): auth_require(bool $json=false), admin_headers(), auth_user(); admin_leads(PDO, q, limit), format_phone(); páginas em public/admin/ fazem require __DIR__ . '/../_bootstrap.php' + APP_PATH . '/lib/auth.php' + '/lib/admin.php'. Botão `data-copy-csv` e link "Baixar CSV" já existem em app/views/admin/panel.php. tests/e2e-admin.sh já faz login e tem helpers (csrf_from, sql, assert_*).
Existentes: csrf_token(), csrf_verify(), e(), db(), now_db(). CSP: script-src 'self', connect-src 'self' (JS só em arquivo externo, sem inline handlers).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e de CSV e status (RED)</name>
  <files>tests/e2e-admin.sh</files>
  <read_first>tests/e2e-admin.sh</read_first>
  <action>Estender `tests/e2e-admin.sh` antes do logout (e um bloco sem sessão com jar novo): (a) sem sessão: GET /admin/export.php → não 200 (302) e POST /admin/status.php com Accept JSON → 401; (b) inserir via sql um lead com name `=HYPERLINK(1)` (demais campos válidos, consent_at/consent_ip/consent_text/ip/created_at preenchidos); (c) logado: GET export.php → 200, header Content-Type contém `text/csv`, primeiros 3 bytes = `efbbbf` (head -c 3 | od -An -tx1 | tr -d ' \n'), primeira linha contém `id;data;nome;telefone;email;mensagem;interesses;status;consent_at;consent_ip`, corpo contém `ana@example.com` e `'=HYPERLINK`; (d) status: pegar id da Ana via sql; POST status.php (Accept JSON) sem csrf → 403; com csrf e status=contatado → 200 com `"ok":true` e sql retorna `contatado`; status=invalido → 422; (e) painel contém `data-copy-csv` e `admin.js`. Rodar e confirmar que falha.</action>
  <verify><automated>LPM_PORT=8091 bash tests/e2e-admin.sh; test $? -ne 0</automated></verify>
  <done>
    - Novos passos (a)–(e) no script, sem editar tests/e2e.sh
    - Falha antes da implementação (RED)
  </done>
</task>

<task type="auto">
  <name>Task 2: export.php, status.php, admin.js e select de status</name>
  <files>public/admin/export.php, public/admin/status.php, public/assets/js/admin.js, app/lib/admin.php, app/views/admin/panel.php, public/assets/css/admin.css</files>
  <read_first>app/lib/admin.php, app/views/admin/panel.php, public/admin/index.php</read_first>
  <action>`app/lib/admin.php`: `csv_safe(?string): string` prefixa `'` se o valor começar com = + - @ TAB ou CR; `LEAD_STATUSES = ['novo','contatado','fechado']`. `export.php`: admin_headers(); auth_require(); Content-Type `text/csv; charset=utf-8`, Content-Disposition attachment `leads-YYYY-MM-DD.csv`; escreve BOM "\xEF\xBB\xBF" e usa fputcsv(php://output, ..., ';', '"', '\\', "\r\n"); cabeçalho id;data;nome;telefone;email;mensagem;interesses;status;consent_at;consent_ip; todos os leads ORDER BY created_at DESC (iterar o statement, sem limite); interesses = títulos do JSON unidos por ", "; toda célula passa por csv_safe. `status.php`: admin_headers(); só POST (405); json = Accept contém application/json; auth_require($json); csrf_verify($_POST['csrf_token']) ou 403; id ctype_digit e status in LEAD_STATUSES senão 422; `UPDATE leads SET status = ? WHERE id = ?` (prepared); JSON `{"ok":true}` ou 303 para `/admin/`. No painel, a coluna Status vira form POST status.php (hidden csrf_token e id, select com os 3 status, botão "Salvar" para uso sem JS). `admin.js` (sem inline, carregado com defer): botão `[data-copy-csv]` faz fetch('/admin/export.php', {credentials:'same-origin'}), remove BOM, navigator.clipboard.writeText, texto vira "Copiado!" por 2s (erro → "Não foi possível copiar"); nos forms de status, esconde o botão Salvar e no change do select envia via fetch (FormData, Accept JSON) mostrando "Salvo" ou revertendo com "Erro". Ajustar admin.css só para o select/feedback.</action>
  <verify><automated>LPM_PORT=8091 bash tests/e2e-admin.sh</automated></verify>
  <done>
    - "E2E ADMIN OK": CSV com BOM, cabeçalho, ';' e proteção contra fórmula
    - Status persiste; sem CSRF 403, inválido 422, sem sessão 401/302
    - admin.js copia o CSV e salva status sem recarregar
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: conferência visual e de uso</name>
  <files>nenhum (só conferência)</files>
  <action>Subir o stack com o e2e e pausar para o dono conferir visual e uso real contra a referência.</action>
  <verify><automated>LPM_PORT=8091 bash tests/e2e-admin.sh</automated></verify>
  <done>Dono respondeu "aprovado".</done>
  <what-built>Painel completo: login, KPIs, radar, fila com busca e status, Baixar/Copiar CSV, Sair.</what-built>
  <how-to-verify>
    1. `LPM_PORT=8091 bash tests/e2e-admin.sh` e abrir http://localhost:8091/admin/ (dono@example.com / SenhaForte123!).
    2. Comparar com `.planning/references/painel-admin-ref.png` (fundo escuro, card branco arredondado, KPIs com borda fina, rodapé LGPD), também no mobile.
    3. Clicar "Copiar CSV" (aparece "Copiado!") e colar num editor; clicar "Baixar CSV" e abrir no Excel (acentos certos, colunas separadas).
    4. Trocar um status, recarregar e conferir que ficou salvo; clicar "Sair" e confirmar que /admin/ volta ao login.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o problema</resume-signal>
</task>

</tasks>

<threat_model>
| ID | STRIDE | Componente | Disposição | Mitigação |
|----|--------|-----------|-----------|-----------|
| T-02-06 | I | export.php | mitigate | auth_require antes de qualquer saída; no-store; noindex |
| T-02-07 | T | CSV | mitigate | csv_safe prefixa ' em = + - @ TAB CR |
| T-02-08 | T | status.php | mitigate | POST + csrf_verify + whitelist + prepared UPDATE; auth_require(json) |
| T-02-09 | E | admin.js | mitigate | CSP script-src 'self'; sem inline; conteúdo renderizado via e() |
</threat_model>

<verification>
`LPM_PORT=8091 bash tests/e2e-admin.sh` imprime "E2E ADMIN OK"; checkpoint visual aprovado.
</verification>

<success_criteria>
CSV baixado/copiado abre certo no Excel, status editável persiste, endpoints protegidos, visual aprovado contra a referência.
</success_criteria>

<output>
Criar `.planning/phases/02-painel-do-dono/02-02-SUMMARY.md` ao concluir.
</output>
